docker-compose-yaml: docker-compose.yml

project: static-site

environments:
  dev:
    routes:
      - nginx:
          - url: ${LAGOON_ROUTE}
            tls-acme: true
            insecure: Redirect
