docker-compose-yaml: docker-compose.yml

project: static-site

environments:
  main:
    routes:
      - nginx:
        - "nginx.${LAGOON_PROJECT}.jp1.amazee.io":
            tls-acme: true
            insecure: Redirect
