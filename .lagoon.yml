docker-compose-yaml: docker-compose.yml

project: static-site

environments:
  dev:
    routes:
      - nginx:
        - "nginx.dev.demo-annai-static-site.jp1.amazee.io":
            tls-acme: true
            insecure: Redirect
            hstsEnabled: true
