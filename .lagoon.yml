docker-compose-yaml: docker-compose.yml

project: static-site

environments:
  dev:
    routes:
      - nginx:
          - "${LAGOON_ROUTE}":
              tls-acme: true
              insecure: Redirect
              basic_auth: true
