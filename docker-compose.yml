x-volumes:
  &default-volumes
    volumes:
      - ./src:/app/src:delegated
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:delegated

x-environment:
  &default-environment
    LAGOON_ROUTE: &default-url http://demo-annai-static-site.docker.amazee.io

x-user:
  &default-user
    user: '1000'

services:
  nginx:
    build:
      context: .
      dockerfile: lagoon/nginx.dockerfile
    labels:
      lagoon.type: nginx
      lagoon.project: static-site
      amazeeio: true
      amazeeio.type: nginx
    << : [*default-volumes, *default-user]
    ports:
      - "8080"
    environment:
      << : *default-environment
      AMAZEEIO: true
      AMAZEEIO_URL: nginx.${LAGOON_ENVIRONMENT}.demo-annai-static-site.jp1.amazee.io
      LAGOON_LOCALDEV_URL: *default-url
    networks:
      - amazeeio-network
      - default

networks:
  amazeeio-network:
    external: true
