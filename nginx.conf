server {
    listen 80;
    server_name nginx.main.demo-annai-static-site.jp1.amazee.io;

    user nginx;

    root /app/src;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
        allow all;
    }
}
